# Performance

The benchmark measures local throughput and latency. Record host, Python version, partitions,
workers, queue size and workload when reporting results. Never present local results as
production capacity.
